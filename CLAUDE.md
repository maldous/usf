# Claude execution shim

Read `AGENTS.md` first. It is the repository process directive and points to the live semantic authority; this shim does not restate USF policy.

- Perform the mandatory bootstrap and foundational reading required by `AGENTS.md`.
- Use bounded delegation and give workers compact task packets.
- Only the coordinator may mutate live semantic state.
- Follow Claude approval and tool-invocation rules without weakening fail-closed repository policy.
- Stop on unresolved authority conflicts and report the required task outcome fields.
