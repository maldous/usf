export const sourceUseTreatments = Object.freeze([
  "source-derived-adapt",
  "source-derived-rewrite",
  "new-with-rationale",
  "evidence-only-support",
] as const);

export const sourceRuntimeImportAllowed = false;
export const sourcePathMirroringAllowed = false;
