# React Operator Admin Surface Equivalence

This artefact supports USF-291 only. It does not claim React UI parity, product UI readiness, Staging readiness, Production readiness, SOC readiness, ISO certification, enterprise production readiness, browser E2E readiness, or full product parity.

|id|surface|disposition|accessMethod|screenshotEvidence|
|---|---|---|---|---|
|react-operator-surface-postgres-admin|pgAdmin or equivalent DB admin posture|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-minio-console|MinIO console or equivalent object storage admin posture|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-grafana|Grafana dashboards|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-sonarqube|SonarQube UI/policy/hotspot posture|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-sentry|Sentry issue/event/alert posture|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-temporal-ui|Temporal UI|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-windmill-ui|Windmill UI|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-mailpit|Mailpit UI|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-clickhouse|ClickHouse interfaces|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-prometheus|Prometheus UI/targets/metrics|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-loki|Loki log query UI|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-tempo|Tempo trace UI|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-alertmanager|Alertmanager UI/routing state|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-openbao|OpenBao-style secrets console/status/policy posture|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-keycloak|Keycloak/SSO admin console|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-gateway-forward-auth|Caddy/gateway/forward-auth operator surface|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-webhook-sink|webhook sink UI|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|react-operator-surface-meilisearch|Meilisearch/search admin surface|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|

