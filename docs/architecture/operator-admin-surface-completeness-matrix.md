# USF Operator Admin Surface Completeness Matrix

This artefact supports USF-291 only. It does not claim UI completeness, product UI readiness, Staging readiness, Production readiness, SOC readiness, ISO certification, enterprise production readiness, browser E2E readiness, or full product completeness.

|id|surface|disposition|accessMethod|screenshotEvidence|
|---|---|---|---|---|
|operator-surface-postgres-admin|pgAdmin or equivalent DB admin posture|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-minio-console|MinIO console or equivalent object storage admin posture|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-grafana|Grafana dashboards|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-sonarqube|SonarQube UI/policy/hotspot posture|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-sentry|Sentry issue/event/alert posture|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-temporal-ui|Temporal UI|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-windmill-ui|Windmill UI|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-mailpit|Mailpit UI|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-clickhouse|ClickHouse interfaces|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-prometheus|Prometheus UI/targets/metrics|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-loki|Loki log query UI|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-tempo|Tempo trace UI|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-alertmanager|Alertmanager UI/routing state|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-openbao|OpenBao-style secrets console/status/policy posture|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-keycloak|Keycloak/SSO admin console|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-gateway-forward-auth|Caddy/gateway/forward-auth operator surface|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-webhook-sink|webhook sink UI|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|
|operator-surface-meilisearch|Meilisearch/search admin surface|covered-by-existing-usf-proof|SSO or authorised service login where safe; API/CLI evidence where UI screenshots would expose secrets or are unavailable|USF-290 machine QA service screenshot manifest where safe; gap marker or CLI/API evidence otherwise|

