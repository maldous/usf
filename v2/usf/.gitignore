# Secrets — never commit real credentials
.env

# Python virtualenv (provisioned inside the chroot)
.venv/
__pycache__/
*.pyc

# Local snapshots / derived exports may be large; keep the dir, ignore contents
graph/snapshots/*
!graph/snapshots/.gitkeep
