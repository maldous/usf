{
  "id": "interface.jobs-cancel-api",
  "title": "Jobs Cancel API Contract",
  "description": "Draft USF-native interface contract for POST /v1/jobs/{id}/cancel. It promotes the implemented local route metadata into canonical interface-contract coverage while preserving OpenAPI as a lower-authority projection.",
  "authorityLevel": "semantic-definition",
  "lifecycleState": "draft",
  "ontologyConcepts": [
    "Interface",
    "Service",
    "Port"
  ],
  "taxonomyRefs": [
    "interface-classification"
  ],
  "vocabularyRefs": [
    "interface-kinds"
  ],
  "aiGuidance": "Treat this interface contract as the route authority. Do not infer semantic authority from OpenAPI, route metadata, examples, generated reports, or handler structure.",
  "interfaceKind": "api-route",
  "purpose": "Represent jobs.cancel as a governed POST API route contract for job-service.",
  "boundary": "Local dev/test inbound HTTP boundary POST /v1/jobs/{id}/cancel; implementation path apps/api/src/server.ts; route classification tenant-scoped.",
  "semanticContractRef": "semantic-contract.scheduled-jobs-built-in-on-the-event-substrate",
  "requestContract": {
    "description": "Request semantics for jobs.cancel bind method, path, operation id, request schema, auth scheme, tenant scope, and required action before implementation handling. Request schema: TenantActionRequest.",
    "fields": [
      "routeId:jobs.cancel",
      "method:POST",
      "path:/v1/jobs/{id}/cancel",
      "openapiOperationId:cancelJobV1",
      "requestSchema:TenantActionRequest",
      "authScheme:dev-header-keycloak-bearer-future",
      "tenantScope:tenant-header-body",
      "requiredAction:job.cancel"
    ]
  },
  "responseContract": {
    "description": "Response semantics for jobs.cancel expose 200 ActionResultResponse, 400 ApiErrorResponse, 403 ApiErrorResponse, 404 ApiErrorResponse; error responses use ApiErrorResponse. Audit, PDP, idempotency, rate-limit, correlation, field exposure, observability, gateway, and proof boundaries remain explicit contract terms.",
    "fields": [
      "responseSchema:200:ActionResultResponse",
      "responseSchema:400:ApiErrorResponse",
      "responseSchema:403:ApiErrorResponse",
      "responseSchema:404:ApiErrorResponse",
      "errorSchema:ApiErrorResponse",
      "auditPolicy:job.cancel audited by job service",
      "pdpPolicy:job service calls PDP",
      "proofEvidenceStatus:bounded-local-dev-test-no-public-api-claim"
    ]
  },
  "permissions": [
    "job.cancel"
  ],
  "security": "Auth scheme dev-header-keycloak-bearer-future; tenant scope tenant-header-body; PDP policy job service calls PDP; audit policy job.cancel audited by job service; idempotency policy required; pagination policy none; rate-limit policy local-dev-test quotas represented; live enforcement deferred; field exposure policy minimum necessary fields; sensitive internals redacted; observability policy tenant-safe request_count error_count validation_failure_count auth_denial_count; gateway policy local-dev-test-only gateway-edge-waf-tls-production-posture-deferred; error schema ApiErrorResponse; proof status is bounded local-dev-test evidence with no public API or live-provider claim.",
  "driftDetectable": true,
  "sourceRefs": [
    "packages/contracts/src/api-surface.ts#routeId=jobs.cancel",
    "packages/openapi/openapi.json#operationId=cancelJobV1",
    "apps/api/src/server.ts"
  ]
}
