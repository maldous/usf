{
  "id": "config.secret-bearing-db-password",
  "configurationKind": "secret-bearing-setting",
  "title": "Database password secret-bearing setting",
  "description": "A secret-bearing setting that carries no literal value; the secret is resolved at runtime from the secret store.",
  "authorityLevel": "semantic-definition",
  "lifecycleState": "draft",
  "ontologyConcepts": ["Configuration"],
  "taxonomyRefs": ["configuration-classification"],
  "vocabularyRefs": ["configuration-kinds"],
  "aiGuidance": "Never serialise the literal value of a secret-bearing setting; resolve it from the secret store at runtime.",
  "purpose": "Hold the database password reference without leaking the secret value.",
  "key": "DATABASE_PASSWORD",
  "secretBearing": true
}
