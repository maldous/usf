{
  "id": "config.neg-secret-bearing-with-value",
  "configurationKind": "secret-bearing-setting",
  "title": "Secret-bearing setting leaking a literal value",
  "description": "DEFECT: secretBearing true but a literal value is present, which would leak the secret.",
  "authorityLevel": "semantic-definition",
  "lifecycleState": "draft",
  "ontologyConcepts": ["Configuration"],
  "taxonomyRefs": ["configuration-classification"],
  "vocabularyRefs": ["configuration-kinds"],
  "aiGuidance": "Never serialise the literal value of a secret-bearing setting.",
  "purpose": "Hold the database password.",
  "secretBearing": true,
  "value": "hunter2"
}
